# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# temp
temp

# env
.env.*
!.env.example
!.env.test

# clade-managed runtime state / build cache
# 注意 .claude/ 是專案 source of truth（rules / skills / hooks 由 hub:sync 治理），
# 只 ignore runtime artifact，不要整個 ignore 整個目錄。
.claude/scheduled_tasks.lock
.claude/.commit.lock
.claude/.commit.lock.history/
template/.wrangler/

# 本地 MCP server 設定（含個人 absolute path，不適合共享）
.mcp.json

# Codex 投影層（sync-to-codex.ts 從 .claude/ 產生，可重生 → 不進版控）
.agents/
.codex/
