# Tasks

## 1. CLI contract

- [x] 1.1 Add and validate `--repo-id` and `--workflow-model`.
- [x] 1.2 Pass fleet identity into post-scaffold processing without changing stack-selection interactivity.

## 2. Clade integration

- [x] 2.1 Resolve the current TypeScript consumer initializer with an older-checkout fallback.
- [x] 2.2 Replace direct `consumers.local` mutation with `register-consumer.ts` invocation.
- [x] 2.3 Emit exact continuation steps when central registration is incomplete.

## 3. Verification and documentation

- [x] 3.1 Add focused tests for script resolution, registration argv, and missing repo identity.
- [x] 3.2 Update CLI and Clade integration documentation.
- [x] 3.3 Run package tests, typecheck, and an isolated scaffold smoke test.
