schema: spec-driven

context: |
  Tech stack: Nuxt 4, Vue 3, TypeScript, Supabase (PostgreSQL)
  Linting: OXLint + OXFmt (not ESLint)

  Critical constraints:
  - Auth uses @onmax/nuxt-better-auth, NEVER Supabase Auth APIs
  - Client-side: SELECT only via useSupabaseClient()
  - Server-side: All writes via /api/v1/* endpoints
  - RLS policies MUST include service_role bypass for API writes
  - Migrations via CLI only (supabase migration new), never manual .sql files

rules:
  proposal:
    - Use Summary → Motivation → Scope → Impact structure
    - Identify affected tables, APIs, and UI components
    - Flag if migration is required
  specs:
    - Use "# Spec: <name>" title format
    - Use WHEN/THEN/AND format for scenarios
    - Include error handling and edge cases
  design:
    - Follow Data Access Pattern (client reads, server writes)
    - RLS policies must include service_role bypass
    - UI changes MUST use Nuxt UI components (UButton, UCard, UTable, etc.)
    - All color/spacing MUST use design system tokens, never hardcoded values
    - Read .impeccable.md for design direction before making UI decisions
  tasks:
    - Follow TDD: write test first, verify red, implement, verify green
    - Each task should be independently verifiable
    - When any task touches UI paths (`.vue`, `pages/`, `components/`, `layouts/`), the task group MUST include a Design Review section before 人工檢查
    - Design Review section MUST include: impeccable check, /design improve, DRIFT fix loop, targeted skills, /audit, /review-screenshot
    - 'UI-affecting tasks MUST include a `> QA:` annotation listing key screens/routes and manual test focus points'

  design_workflow:
    - Design Review is mandatory for UI-touching changes; it is not optional polish
    - /impeccable teach runs once per project if .impeccable.md is missing
    - /design improve produces a Design Fidelity Report; all DRIFT items must be fixed before proceeding
    - Fidelity fix loop has max 2 rounds; if still failing, escalate to user
    - /audit must reach Critical = 0 before marking Design Review complete
    - /review-screenshot captures visual evidence for archive
    - Design Review findings are logged to docs/design-review-findings.md for cross-change pattern analysis
