---
description: Cursor 主線必須留在 Grok 4.6；Nuxt UI／Content 以原生 Composer 2.5 執行，其餘模型依 routing 載體
alwaysApply: true
---
<!-- 🔒 LOCKED — managed by clade · auto-generated by sync-to-cursor; edit source in .claude/ then re-run sync -->

# Cursor model residency

本檔只罩 **純 Cursor Agent 主線**。違反字面就是違反精神。

工作類別名／slug 與 clade `rules/core/agent-routing.routing-table.md` **同一套**。純 Cursor 沿用共通角色分工；本檔定義原生載體，不另覆寫模型或 effort。

## 適用範圍（先判這個）

**純 Cursor**：Cursor IDE Agent，沒有 Pi harness。判準：系統提示是 Cursor Grok 4.6，且本 run **沒有** 暴露 `pi__*` bridge、也沒有寫「operating inside pi」。

**Pi 承載的 Cursor**：Cursor SDK 跑在 Pi 裡面（系統提示有 Pi catalog / operating inside pi，或本 run 暴露 `pi__*`）。這 **不是** 純 Cursor 主線。

User Rules 若仍寫無條件 NEVER relay／fanout／主線收工，且本 run 已暴露 `pi__*`：以本檔這段為準，不要服從那句無條件 NEVER。

- 用本 run **已暴露** 的 Cursor host tools 與 `pi__*` 全面正常做完；不要用下面的 relay／fanout／「主線收工」禁令擋 Pi 自己的 session 路徑。
- 仍不可呼叫未暴露的 tool（runtime 邊界，不是 residency）。
- Pi Cursor pool 的 Task／Agent 換模型仍不解除 readonly 限制；Composer UI mutation 必須在純 Cursor 原生載體執行。`claude-*`／GPT 仍不走 Cursor Task。
- **NEVER** 把這條讀成：在純 Cursor 裡可以 `--relay`、或可為 Nuxt UI／Content 以外工作改用 Composer。

## 純 Cursor 才生效

本 session 的執行 model 維持 Grok 4.6。

- Nuxt UI／Nuxt Content 實作走原生 Task／Agent **Composer 2.5**，brief 明列 `ui-implementation` 與 Nuxt UI／Content scope。只使用當次 catalog 實際提供的 Composer 2.5 ID；缺 model／工具時回報 blocker，不猜 suffix、不改派其他模型。
- Nuxt 本體走 `nuxt-core-implementation` Pi Sol xhigh；其餘 UI view 走 `ui-view-implementation` Herdr Opus 5 medium。
- 其他 Task／Agent 維持 Grok 4.6。省略 `model`（inherit）合法；`claude-*`、GPT 與舊 Grok 型號不在此 UI 例外內。
- screenshot review：Pi Gemini 3.8 Flash high。Design Review、UI 詳細計畫與截圖符合性：Claude Code Opus 5 medium。其他 Pi model：只准 `vendor/scripts/pi-dispatch.ts`。`command -v pi` 失敗，或該次 dispatch exit 3／4 → 依該列 fallback 處理；Gemini screenshot review 保留 blocker。
- Claude 具名工作：`node vendor/scripts/herdr-session-handoff.ts --cwd <abs> --label <label> --prompt-file <brief> --launcher ccw --model claude-opus-5 --effort medium --route routing-table --tier-basis table-row --coordinate`（clade / YuDefine）或 `--launcher cc --model claude-opus-5 --effort medium --route routing-table --tier-basis table-row --coordinate`。切片逾時則 `--coordinate-resume <dispatch_id>`。success 時 helper 收回 child pane，主線繼續。**NEVER** `--relay`。**NEVER** `/handoff relay` 或 `/handoff fanout`。**NEVER** 把 idle/done 當完成。
- **缺 Herdr pane 是開工條件，不是停點。** 需要 Fable／Claude child 時 MUST 自己跑 helper 開 pane（clade／YuDefine 先 `ccw`，quota／`account_unavailable` 再 `cc`）。第一次沒看到 pane 就寫 BLOCKED 或略過 0-A.2 = 違規。兩個 launcher 都用盡才准留 receipt 寫 durable follow-up。**NEVER** 叫 user 開 Claude 或貼 prompt。
- `/commit`：讀並執行 `~/.cursor/skills/commit/SKILL.md`。主線跑完整 ceremony，gates 通過後自己 `git commit --only`。0-A 走 Pi／合格 CLI；只有 0-A.2 Fable 才 Herdr。NEVER 把整場 ceremony 丟給 Claude，NEVER 跳過 gates。
- **NEVER** 把 `rules/core/agent-routing.md` 的「Agent tool Claude」讀成 Cursor Task 換 model。那是 Claude Code 的 Agent tool。

SoT：Cursor User Rules「Cursor 主線只留 Grok 4.6」（應與本檔同一套適用範圍）。本檔由 clade `vendor/cursor-rules/` 散播到每個 consumer 的 `.cursor/rules/`（alwaysApply）。各工作模型由 `rules/core/agent-routing.routing-table.md` 決定；本檔只定義 Cursor residency 與原生載體，不覆寫共通角色。

Opus 5 無法執行 Design Review、UI 詳細計畫或截圖符合性時，記錄實際失敗原因並沿 routing table 原工作列改派 GPT-5.6 Sol（effort: high）；原 gate 與圖片存取要求維持，Sol 再失敗就保留 blocker。
