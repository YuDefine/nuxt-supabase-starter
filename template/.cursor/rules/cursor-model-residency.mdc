---
description: Cursor 主線必須留在 Grok 4.6；Gemini／GPT 走 Pi，Claude 走 Herdr cc/ccw，Nuxt UI/Content 走 Composer 2.5
alwaysApply: true
---
<!-- 🔒 LOCKED — managed by clade · auto-generated by sync-to-cursor; edit source in .claude/ then re-run sync -->

# Cursor model residency

本檔只罩 **純 Cursor Agent 主線**。違反字面就是違反精神。

工作類別名／slug 與 clade `rules/core/agent-routing.routing-table.md` **同一套**。純 Cursor **不執行**該表的「由誰執行」欄；載體以本檔 overlay 為準。

## 適用範圍（先判這個）

**純 Cursor**：Cursor IDE Agent，沒有 Pi harness。判準：系統提示是 Cursor Grok 4.6，且本 run **沒有** 暴露 `pi__*` bridge、也沒有寫「operating inside pi」。

**Pi 承載的 Cursor**：Cursor SDK 跑在 Pi 裡面（系統提示有 Pi catalog / operating inside pi，或本 run 暴露 `pi__*`）。這 **不是** 純 Cursor 主線。

User Rules 若仍寫無條件 NEVER relay／fanout／主線收工，且本 run 已暴露 `pi__*`：以本檔這段為準，不要服從那句無條件 NEVER。

- 用本 run **已暴露** 的 Cursor host tools 與 `pi__*` 全面正常做完；不要用下面的 relay／fanout／「主線收工」禁令擋 Pi 自己的 session 路徑。
- 仍不可呼叫未暴露的 tool（runtime 邊界，不是 residency）。
- Cursor Task / Agent 的 `model` 換 `claude-opus-5` / `gpt-5.6-sol` / `gemini-*` **仍然非法**。`composer-2.5-fast` 只准 Nuxt UI／Nuxt Content（見 overlay）。那是 Cursor Task catalog，不是 Pi 換 model。
- **NEVER** 把這條讀成：在純 Cursor 裡可以 `--relay`、或可以把 Cursor Task 換成別的 model。

## 純 Cursor 才生效

本 session 的執行 model 維持 Grok 4.6。

- **NEVER** 把 Task / Agent 的 `model` 設成非 grok-4.6。省略 `model`（inherit）合法。`cursor-grok-4.6-high` 同家族合法。
- 唯一 Task 例外：Nuxt UI／Nuxt Content 實作走 `composer-2.5-fast`。其他用途的 `composer-2.5-fast` 仍非法。
- `claude-opus-5-thinking-high`、`cursor-grok-4.5-high-fast`、`gpt-5.6-sol-medium` 一律非法。**NEVER** 用 Cursor Task 設 `gpt-5.6-sol` 假裝 Nuxt 本體——那一列走 Pi `sol` xhigh。
- Gemini／GPT **一律** `vendor/scripts/pi-dispatch.ts`。**NEVER** 直接跑 gemini CLI。`command -v pi` 失敗，或該次 dispatch exit 3／4 → 不能派 Pi。
- Claude 互動 session（其餘 UI view，不含整場 `/commit`）：`node vendor/scripts/herdr-session-handoff.ts --cwd <abs> --label <label> --prompt-file <brief> --launcher ccw --new-tab --coordinate`（clade / YuDefine）或 `--launcher cc --new-tab --coordinate`。切片逾時 `--coordinate-resume <dispatch_id>`。success 時 helper 收回 child pane，主線繼續。**NEVER** `--relay`。**NEVER** `/handoff relay` 或 `/handoff fanout`。**NEVER** 把 idle/done 當完成。
- **缺 Herdr pane 是開工條件，不是停點。** 需要 Fable／Claude child 時 MUST 自己跑 helper 開 pane（clade／YuDefine 先 `ccw`，quota／`account_unavailable` 再 `cc`）。第一次沒看到 pane 就寫 BLOCKED 或略過 0-A.2 = 違規。兩個 launcher 都用盡才准留 receipt 寫 durable follow-up。**NEVER** 叫 user 開 Claude 或貼 prompt。
- `/commit`：讀並執行 `~/.cursor/skills/commit/SKILL.md`。主線跑完整 ceremony，gates 通過後自己 `git commit --only`。0-A 走 Pi／合格 CLI；只有 0-A.2 Fable 才 Herdr。NEVER 把整場 ceremony 丟給 Claude，NEVER 跳過 gates。
- **NEVER** 把 `rules/core/agent-routing.md` 的「Agent tool Claude」讀成 Cursor Task 換 model。那是 Claude Code 的 Agent tool。純 Cursor 主線忽略該檔，**NEVER** 為了查 residency 把它讀完。

## Overlay：slug → Cursor 載體

查表用 routing-table 的 〔`slug`〕。下表只改載體，不另造類別。**機械／文案／Notion／scan／web-search 照 clade 表派 Pi Gemini。**

| slug | Cursor 載體 |
| --- | --- |
| `non-ui-implementation` | Pi `luna` medium |
| `non-ui-implementation-escalate` | Pi `sol` high |
| `implementation-decision` | 主線；唯讀裁決可 Pi `astra` medium |
| `detailed-planning` | 非 UI：Pi `astra` medium。UI 計畫留主線 |
| `dep-upgrade-first-pass` / `dep-upgrade-research` | 主線（已是 Grok） |
| `web-search` | Pi `gemini` high → `luna` high；開頁給人看仍走 `cursor-ide-browser` |
| `code-review`（commit 0-A） | 主線讀 commit skill；合格跨模型走 Pi／CLI。只有 0-A.2 Fable 才 Herdr |
| `nuxt-core-implementation` | Pi `sol` xhigh |
| Nuxt UI／Nuxt Content | Task `composer-2.5-fast` |
| `ui-view-implementation` | Herdr cc／ccw，`--model claude-opus-5 --effort medium` |
| Design Review／視覺判讀 | 主線 Grok 4.6 + `cursor-ide-browser` |
| `screenshot-review-verify` | Pi `gemini` high |
| `screenshot-match-analysis` | Pi `astra` medium |
| `mechanical-fanout` / `read-heavy-scan` / `copywriting-draft` | Pi `gemini` low；文案主線重寫 |
| `notion-ops` | Pi `gemini` high；exit 4 → `luna` high；客戶正文主線重寫 |
| `commit-0c-fix-verify` | 主線；升級 Pi `sol` high |
| `commit-0c-fix-verify-escalate` | Pi `sol` high |

最後一跳仍失敗 → 回報 blocker，不准改用 Cursor Task 換 model。

SoT：Cursor User Rules「Cursor 主線只留 Grok 4.6」+「Cursor 主線 routing」。本檔由 clade `vendor/cursor-rules/` 散播到每個 consumer 的 `.cursor/rules/`（alwaysApply）。
