/**
 * Drizzle schema entrypoint.
 *
 * Keep code-first query metadata here when a project opts into Drizzle.
 * Supabase migrations remain the persistence source of truth.
 *
 * Re-export schema tables here, e.g.:
 *   export { usersTable } from './users'
 */
export const schema = {} as const
